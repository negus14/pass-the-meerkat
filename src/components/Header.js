// Header.js
import React from 'react';
import Navbar from './Navbar'; // Adjust the import path if necessary
import './Header.css'; // Import your CSS for styling if needed

const Header = () => {
  return (
    <header className="header">
      <Navbar />
    </header>
  );
};

export default Header;
